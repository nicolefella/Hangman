/** ArrayFunctions.as* Nicole Fella* Assignment 7*/package{	//import statements		/*	* description of class	* "extends" statements would go here	*/		public class ArrayFunctions	{		/**********Instance Properties**********/		//user defined properties for class				/**********Instance Methods**********/				//Constructor		public function ArrayFunctions()		{					}				//Additional instance methods go here				/*the parameter of this function is assumed to be an Array of Strings		Should return a String in concatenation of elements of Array (summing up Array strings)		*/		public function array2String (inputA: Array): String		{			//define String variable concatString, initalize to contain nothing			var concatString: String = "";			//use for-loop to go through each element of Array			for (var i: int = 0; i<inputA.length; i++){				//"sum" up elements of Array into concatString				concatString = concatString + inputA[i];			}			//return concatString			return concatString;		}				/*create Array that puts each letter of passed parameter into own slot*/		public function string2Array (inputS: String): Array		{			//define concatArray variable			var concatArray: Array = new Array(inputS.length);			//use for-loop to go through each element of String			for (var i: int = 0; i<inputS.length; i++){				//put letter at certain place in String into spot in Array				concatArray[i] = inputS.substr(i,1);			}			//return concatArray			return concatArray;		}	}}